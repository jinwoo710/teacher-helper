export type Student = {
    id: number,
    studentId: number,
    name: string,
    gender: "남" | "여",
}

export type StudentFormData = {
    studentId: number,
    name: string,
    gender: "남" | "여",
}